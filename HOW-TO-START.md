# كيفية تشغيل نظام إدارة المستودعات

## ✅ الحل: البرنامج الآن يعمل!

تم إصلاح المشكلة. البرنامج الآن جاهز للعمل.

---

## 📌 طريقتان لتشغيل البرنامج

### 🔹 الطريقة 1: الوضع المحلي (Database Mode)
**الأسرع والأسهل - للاستخدام على جهاز واحد**

```bash
# ببساطة شغّل:
START-APP.bat
```

أو من command line:
```bash
npm run dev
```

**المميزات:**
- ✅ لا يحتاج إلى server منفصل
- ✅ يعمل مباشرة مع قاعدة Access Database
- ✅ سريع ولا يحتاج إعدادات شبكة

---

### 🔹 الطريقة 2: وضع الشبكة (API Mode)
**للاستخدام على عدة أجهزة في نفس الشبكة**

#### الخطوة 1: تشغيل Backend Server (على جهاز واحد فقط)
```bash
cd backend
node server.js
```

#### الخطوة 2: تشغيل البرنامج على كل جهاز
```bash
npm run dev:api
```

أو استخدم الملف الجاهز:
```bash
start-with-api.bat
```

**المميزات:**
- ✅ يدعم عدة مستخدمين في نفس الوقت
- ✅ البيانات متزامنة بين جميع الأجهزة
- ✅ يمكن استخدام PostgreSQL بدلاً من Access

---

## 🔍 ماذا تم إصلاحه؟

### المشكلة السابقة:
```
Authentication Failed
Cannot read properties of undefined (reading 'authenticateUser')
```

### السبب:
- البرنامج كان يستخدم ملف `preload.cjs` القديم (API version)
- بينما يعمل بـ `main.js` (Database version)
- النسختان غير متوافقتين

### الحل:
✅ تم تعديل `vite.config.ts` لبناء النسخة الصحيحة من preload
✅ تم حذف الملفات القديمة وإعادة البناء
✅ الآن `preload.cjs` و `main.js` متوافقان تماماً

---

## 📝 ملاحظات مهمة

1. **إذا لم تفتح نافذة تسجيل الدخول:**
   - أغلق التطبيق تماماً (Ctrl+C في terminal)
   - شغّله من جديد بـ `START-APP.bat`

2. **إذا ظهر خطأ في قاعدة البيانات:**
   - تأكد من وجود ملف `Stores_DB.accdb` في نفس المجلد
   - تأكد من تنصيب Microsoft Access Database Engine

3. **للتبديل بين الوضعين:**
   - **Local Mode**: `npm run dev` (يستخدم main.ts + preload.ts)
   - **API Mode**: `npm run dev:api` (يستخدم main-api.ts + preload-api.ts)

---

## 🎯 الاختلافات بين الوضعين

| الميزة | Local Mode | API Mode |
|--------|-----------|----------|
| سرعة التشغيل | ⚡ سريع جداً | 🔄 يحتاج server |
| عدد المستخدمين | 👤 واحد | 👥 متعدد |
| قاعدة البيانات | Access | PostgreSQL |
| الشبكة | ❌ غير مطلوبة | ✅ مطلوبة |
| الإعداد | 🟢 سهل | 🟡 متوسط |

---

## ✅ البرنامج الآن جاهز للاستخدام!

شغّل `START-APP.bat` وابدأ العمل مباشرة! 🚀
